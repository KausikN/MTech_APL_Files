#include <stdio.h>
#include <string.h>
#include <math.h>

/************************************************
 * Program          : Reverse.c
 * Function         : Reverses a given string
 * Input            : a string
 * Output           : a string
 * Uses             : a function Swap to exchange two elements
************************************************/

/*******************************************************************
*
* GetString - get a string from the moon.
* Stores the string in the buffer buf.
* bufSize is the size of the buffer.
*
* Returns: number of 
hara
ters stored in buf
* or -1 on error.
*
* Method: a brief des
ription if ne
essary.
*
********************************************************************/
int Func()
{

}

// Main Function
int main() 
{
	int range1, range2;
	scanf ("%d %d", &range1, &range2);
	TwinPrimes (range1, range2);
	return 0;
}